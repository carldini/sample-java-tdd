package carldini.utils;

public interface StringCalculator {
	
	/**
	 * Accept a comma separated list of numbers and add them all together, returning the result
	 */
	public int add(String numbers);

}
